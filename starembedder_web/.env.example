# ── Server ────────────────────────────────────────────────────────────────────
# Port the SvelteKit Node server listens on (adapter-node default: 3000)
PORT=3000

# Host to bind to (default: 0.0.0.0)
# HOST=0.0.0.0

# Required when running behind a reverse proxy (for CSRF / cookie security).
# Set to the public-facing URL of the web server.
# ORIGIN=https://yourdomain.com
